<Application x:Class="SegaLauncher.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <!-- ===================== SCENE PALETTE (from mockup :root) ===================== -->
        <SolidColorBrush x:Key="Bg"     Color="#FF0A0B11"/>  <!-- near-black -->
        <SolidColorBrush x:Key="Panel"  Color="#FF0F111C"/>
        <SolidColorBrush x:Key="Panel2" Color="#FF141728"/>
        <SolidColorBrush x:Key="Ink"    Color="#FFECE6D6"/>  <!-- cream, not white -->
        <SolidColorBrush x:Key="Dim"    Color="#FF6B6F8C"/>
        <SolidColorBrush x:Key="Faint"  Color="#FF3A3E57"/>
        <SolidColorBrush x:Key="Blue"   Color="#FF3A5BFF"/>  <!-- sega electric blue -->
        <SolidColorBrush x:Key="Red"    Color="#FFFF3146"/>  <!-- hot alert red -->
        <SolidColorBrush x:Key="Amber"  Color="#FFFFC23A"/>
        <SolidColorBrush x:Key="Green"  Color="#FF57F5A3"/>  <!-- terminal ok -->
        <SolidColorBrush x:Key="Edge"   Color="#FF23273F"/>

        <!-- ===================== FONTS (bundled Resources) ===================== -->
        <FontFamily x:Key="Display">pack://application:,,,/Assets/#Anton</FontFamily>
        <FontFamily x:Key="Term">pack://application:,,,/Assets/#VT323</FontFamily>

        <!-- ===================== WINDOW CHROME ===================== -->
        <!-- minimize -->
        <Style x:Key="Chrome" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Foreground" Value="{StaticResource Dim}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="{StaticResource Term}"/>
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{StaticResource Panel2}"/>
                                <Setter Property="Foreground" Value="{StaticResource Ink}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- close: red wash on hover -->
        <Style x:Key="ChromeClose" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Foreground" Value="{StaticResource Dim}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="{StaticResource Term}"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{StaticResource Red}"/>
                                <Setter Property="Foreground" Value="#FF000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===================== BRUTALIST BUTTON (hard offset shadow, invert on hover) ===================== -->
        <!-- Base: cream fill, ink border, hard 4,4 black shadow (BlurRadius=0 — NO bloom). -->
        <Style x:Key="SceneButton" TargetType="Button">
            <Setter Property="Foreground" Value="#FF06060E"/>
            <Setter Property="Background" Value="{StaticResource Ink}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Ink}"/>
            <Setter Property="FontFamily" Value="{StaticResource Display}"/>
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="22,11"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="b"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="2"
                                    Padding="{TemplateBinding Padding}"
                                    RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <TranslateTransform/>
                                </Border.RenderTransform>
                                <Border.Effect>
                                    <DropShadowEffect Color="#FF000000" Direction="-45"
                                                      ShadowDepth="5" BlurRadius="0" Opacity="1"/>
                                </Border.Effect>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"
                                                  TextElement.FontFamily="{TemplateBinding FontFamily}"
                                                  TextElement.FontSize="{TemplateBinding FontSize}"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <!-- invert: fill -> bg, text -> ink -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{StaticResource Bg}"/>
                                <Setter Property="Foreground" Value="{StaticResource Ink}"/>
                                <Setter TargetName="b" Property="RenderTransform">
                                    <Setter.Value><TranslateTransform X="-1" Y="-1"/></Setter.Value>
                                </Setter>
                            </Trigger>
                            <!-- press: shove down-right into the shadow -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="b" Property="RenderTransform">
                                    <Setter.Value><TranslateTransform X="2" Y="2"/></Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="b" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Blue variant -->
        <Style x:Key="SceneButton.Blue" TargetType="Button" BasedOn="{StaticResource SceneButton}">
            <Setter Property="Background" Value="{StaticResource Blue}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Blue}"/>
            <Setter Property="Foreground" Value="#FFFFFFFF"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource Blue}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Red variant -->
        <Style x:Key="SceneButton.Red" TargetType="Button" BasedOn="{StaticResource SceneButton}">
            <Setter Property="Background" Value="{StaticResource Red}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Red}"/>
            <Setter Property="Foreground" Value="#FF0A0A0A"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource Red}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Ghost variant: transparent, edge border, no shadow -->
        <Style x:Key="SceneButton.Ghost" TargetType="Button" BasedOn="{StaticResource SceneButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource Edge}"/>
            <Setter Property="Foreground" Value="{StaticResource Ink}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="2"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"
                                              TextElement.FontFamily="{TemplateBinding FontFamily}"
                                              TextElement.FontSize="{TemplateBinding FontSize}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{StaticResource Panel2}"/>
                                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Ink}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="b" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- dashed amber "request" button (matches mockup .reqbtn) -->
        <Style x:Key="ReqButton" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource Amber}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FontFamily" Value="{StaticResource Display}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" BorderThickness="2" Padding="11,10"
                                Background="{TemplateBinding Background}">
                            <Border.BorderBrush>
                                <SolidColorBrush Color="#FF23273F"/>
                            </Border.BorderBrush>
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Amber}"/>
                                <Setter TargetName="b" Property="Background" Value="#14FFC23A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- sidebar release item (matches .rel) -->
        <Style x:Key="ReleaseTab" TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="{StaticResource Ink}"/>
            <Setter Property="FontFamily" Value="{StaticResource Term}"/>
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="bd" BorderThickness="2" BorderBrush="Transparent"
                                Background="Transparent" Padding="10,9">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource Panel2}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource Blue}"/>
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource Panel2}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- scene text input (matches modal input/textarea) -->
        <Style x:Key="SceneInput" TargetType="TextBox">
            <Setter Property="Background" Value="#FF06070E"/>
            <Setter Property="Foreground" Value="{StaticResource Ink}"/>
            <Setter Property="CaretBrush" Value="{StaticResource Ink}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Edge}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontFamily" Value="{StaticResource Term}"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="b" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Blue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
