namespace SegaLauncher;

// Stamped by package.ps1 at publish time with the SHA-256 of the shipped game.enc.
// Empty string = dev build, so the integrity check is skipped (local dotnet run works).
public static class BuildInfo
{
    public const string ExpectedBlobSha256 = "";
}

